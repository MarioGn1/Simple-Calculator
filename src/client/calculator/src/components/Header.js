const Header = () => {

    return (
        <header id="site-header">
            <nav className="navbar">
                <section className="navbar-dashboard">
                    <div>Calculator</div>
                </section>
            </nav>
        </header>
    );
}

export default Header;